# Copy to infra/.env — never commit .env
KC_ADMIN_PASSWORD=change-me-admin

# Must match passwords embedded in keycloak/import/study-reservation-realm.json
# (local lab only — change both together if you rotate)
TEST_USER_PASSWORD=changeme-test
